# Jaddress-web-application

Quick guide to run the project locally.

## Structure
- `my-app/` – Next.js + TypeScript app.

## Requirements
- Node.js 18+ (20 recommended).
- npm (bundled with Node.js).

## First run
1. Open a terminal in the repo root.
2. Install dependencies: `cd my-app && npm install`.
3. Start dev server: `npm run dev`.
4. Open `http://localhost:3000` in your browser.
5. Stop the server with `Ctrl+C` in the terminal.

## Build/production
- Build: `npm run build`
- Start production build: `npm start` (after `npm run build`)

## Lint
- Code check: `npm run lint`
